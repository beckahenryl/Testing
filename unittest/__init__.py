def sum(arg):
	total = 0
	for val in arg:
		total+= val
	return total
#create variable total that iterates over all values
#and adds them to total. It then returns the result once
#the iterable has been exhausted		